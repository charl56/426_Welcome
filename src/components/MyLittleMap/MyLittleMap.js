import React from "react";// Libs for leaflet & Co
// CSS
import './MyLittleMap.css';
// Components

const MyLittleMap = React.memo(() => {
    
    

    return (
        <div className="map__little--screen">
        </div>
    );
})

export default MyLittleMap;